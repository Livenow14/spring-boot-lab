bootJar { enabled = false }
jar { enabled = true }

dependencies {
    implementation(project(":management-core"))
    testImplementation(testFixtures(project(":management-core")))
    implementation 'org.springframework.boot:spring-boot-starter'
}
